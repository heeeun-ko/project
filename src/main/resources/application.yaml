server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/finset
    username: chorok
    password: 789789
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update # ??? update ? ?? validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace